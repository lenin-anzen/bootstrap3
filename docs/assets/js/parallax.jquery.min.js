/**
 * @author: Heather Corey
 * @description: jQuery Simple Parallax Plugin
 */
!(function (a) {
  a.fn.parallax = function (b) {
    let c = a(window).height(),
      d = a.extend({ speed: 0.15 }, b); return this.each(function () {
      const b = a(this); a(document).scroll(() => {
        let e = a(window).scrollTop(),
          f = b.offset().top,
          g = b.outerHeight(); if (!(f + g <= e || f >= e + c)) { const h = Math.round((f - e) * d.speed); b.css('background-position', `center ${h}px`); }
      });
    });
  };
}(jQuery));
